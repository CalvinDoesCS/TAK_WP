# Vite environment variables
VITE_API_URL=http://localhost:8080/wp-json
VITE_APP_NAME=Octarine Admin Dashboard

# WordPress API base URL for WASM modules
# Leave empty to use relative path (recommended with Vite proxy)
# Or set to http://localhost:8080 for direct calls
VITE_WP_API_BASE=

# Port for the Vite dev server (default: 5173)
PORT=5173
